/**
 * Orchestrator Module Entry Point
 * 
 * The Orchestrator is the central coordinator for the multi-agent system.
 * It receives user input, delegates to specialized agents, and aggregates
 * their outputs into a coherent response.
 */

export { Orchestrator, orchestrator } from "./orchestrator";
